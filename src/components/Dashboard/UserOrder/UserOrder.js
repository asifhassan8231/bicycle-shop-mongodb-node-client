import { Container, Typography } from '@mui/material';
import React from 'react';

const UserOrder = () => {
    return (
        <Container>
            <Typography>User Order</Typography>
        </Container>
    );
};

export default UserOrder;