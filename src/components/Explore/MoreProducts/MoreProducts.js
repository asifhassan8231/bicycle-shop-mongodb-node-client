import { Container, Typography } from '@mui/material';
import React from 'react';

const MoreProducts = () => {
    return (
        <Container>
            <Typography>
                more products
            </Typography>
        </Container>
    );
};

export default MoreProducts;